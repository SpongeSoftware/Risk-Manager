name: CI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  typecheck:
    name: typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: pnpm/action-setup@0c17529a66aca453f9227af23103ed11469b1e47 # v4.0.0

      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm typecheck
        env:
          APP_ENV: production
          TURSO_DATABASE_URL: file:./data/db.sqlite
          WORKOS_CLIENT_ID: ci-placeholder
          WORKOS_API_KEY: ci-placeholder
          WORKOS_REDIRECT_URI: http://localhost:3000/callback
          WORKOS_COOKIE_PASSWORD: ci-placeholder-32-chars-padded-000
          SENTRY_ORG: ci-placeholder
          SENTRY_PROJECT: ci-placeholder
          SENTRY_AUTH_TOKEN: ci-placeholder
          VITE_APP_ENV: production

  lint:
    name: lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: pnpm/action-setup@0c17529a66aca453f9227af23103ed11469b1e47 # v4.0.0

      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate route types
        run: pnpm exec react-router typegen
        env:
          APP_ENV: production
          TURSO_DATABASE_URL: file:./data/db.sqlite
          WORKOS_CLIENT_ID: ci-placeholder
          WORKOS_API_KEY: ci-placeholder
          WORKOS_REDIRECT_URI: http://localhost:3000/callback
          WORKOS_COOKIE_PASSWORD: ci-placeholder-32-chars-padded-000
          VITE_APP_ENV: production

      - name: Lint
        run: pnpm lint

  build:
    name: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: pnpm/action-setup@0c17529a66aca453f9227af23103ed11469b1e47 # v4.0.0

      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build
        env:
          APP_ENV: production
          TURSO_DATABASE_URL: file:./data/db.sqlite
          WORKOS_CLIENT_ID: ci-placeholder
          WORKOS_API_KEY: ci-placeholder
          WORKOS_REDIRECT_URI: http://localhost:3000/callback
          WORKOS_COOKIE_PASSWORD: ci-placeholder-32-chars-padded-000
          SENTRY_ORG: ci-placeholder
          SENTRY_PROJECT: ci-placeholder
          SENTRY_AUTH_TOKEN: ci-placeholder
          VITE_APP_ENV: production
